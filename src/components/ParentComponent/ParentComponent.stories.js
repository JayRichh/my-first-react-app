/* eslint-disable */
import ParentComponent from './ParentComponent';

export default {
  title: "ParentComponent",
};

export const Default = () => <ParentComponent />;

Default.story = {
  name: 'default',
};
